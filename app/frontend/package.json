{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "npx serve -s build",
    "build": "npx esbuild src/index.js --bundle --outfile=build/index.js --loader:.js=jsx"
  }
}
